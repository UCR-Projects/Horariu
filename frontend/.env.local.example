# API Environment Configuration
# Copy this file to .env.local and uncomment the desired option

# Use local backend (requires: cd backend && npm run dev:local)
# VITE_API_ENV=local

# Use AWS Lambda Dev environment
# VITE_API_ENV=dev

# Use AWS Lambda Prod environment (default for production builds)
# VITE_API_ENV=prod

